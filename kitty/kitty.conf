allow_remote_control socket-only
listen_on unix:/tmp/kitty

cursor_shape     block
shell_integration no-cursor
cursor_blink_interval 0.5

enabled_layouts splits

draw_minimal_borders      yes
sync_to_monitor no
strip_ansi_hyperlinks off

# kitty-scrollback.nvim Kitten alias
action_alias kitty_scrollback_nvim kitten '~/.local/share/nvim/lazy/kitty-scrollback.nvim/python/kitty_scrollback_nvim.py'

map ctrl+s kitty_scrollback_nvim

map ctrl+space launch --cwd=current zed .
map ctrl+shift+t launch --type=tab --cwd=current
map alt+h launch --type=window --location=vsplit --cwd=current
map alt+l launch --type=window --location=hsplit --cwd=current
map alt+k next_window
map alt+j prev_window

map ctrl+y copy_selection_or_last_command_output

scrollback_lines 500

font_family      GeistMono NFM Light
bold_font        GeistMono NFM Light
italic_font      auto
bold_italic_font auto
font_size        14
modify_font line_height 130%

hide_window_decorations yes

active_border_color       #bd93f9
inactive_border_color     #6272a4

# Core Colors
background            #000000
# foreground            #cbd3cc

# foreground            #9ca4ae
foreground            #adb5bd
cursor                #cbd3dd
selection_background  #364456
selection_foreground  #ccd4e0

# Black & Muted Gray
color0                #181a1f
color8                #4b5263

# Red (Soft/Muted Crimson)
color1                #f2717a
color9                #f2717a

# Green (Sage / Emerald Mint - removes the harsh neon)
color2                #7bc293
color10               #7bc293

# Yellow (Soft Ochre / Gold - fixes the yellowish tint)
color3                #ddb67e
color11               #ddb67e

# Blue (Deep Steel Blue)
color4                #5c7b99
color12               #5c7b99

# Magenta (Soft Mauve)
color5                #b48ead
color13               #b48ead

# Cyan (Teal / Mint Blue)
color6                #5faaba
color14               #5faaba

# White (Stealth Matchmaker)
color7                #4b5263
color15               #565f73
